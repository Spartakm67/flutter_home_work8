import 'package:flutter/material.dart';

class SnackBarStyle {
  static const TextStyle snackBarTextStyle = TextStyle(
    color: Colors.white,
    fontWeight: FontWeight.bold,
    fontSize: 24,
  );

  static const double heightFactor = 1.0;

  static const Color backgroundColor = Colors.redAccent;

  static const Duration displayDuration = Duration(seconds: 2);
}